# Example env for BANJA backend
# Configure these in Render (or your host) — do NOT commit real secrets

MONGO_URI=mongodb+srv://<user>:<pw>@cluster.example.mongodb.net/banja
JWT_SECRET=replace_with_a_secure_random_string
FRONTEND_URL=https://your-frontend.example.com
UPLOAD_DIR=uploads
